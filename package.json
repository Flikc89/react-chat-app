{
  "name": "chat-app",
  "version": "1.0.0",
  "main": "index.js",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix mock-server\" \"npm run dev --prefix frontend\"",
    "dev:mock": "npm run dev --prefix mock-server",
    "dev:frontend": "npm run dev --prefix frontend",
    "start": "concurrently \"npm run start --prefix mock-server\" \"npm run preview --prefix frontend\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
